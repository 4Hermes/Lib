/**
 * This interface defines the object that contains the username & password for
 * interacting with an API that supports a form of basic authentication.
 */
export interface IBasicCredentials {
  username: string
  password: string
}
