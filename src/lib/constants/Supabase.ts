import { ISupabaseConfig, UNKNOWN_VALUE } from '../../index.js'

/**
 * An object containing all of the default values to use for the
 * {@link ISupabaseConfig supabaseConfig} object.
 */
export const DEFAULT_SUPABASE_CONFIG: ISupabaseConfig = {
  url: UNKNOWN_VALUE,
  apiKey: UNKNOWN_VALUE,
}
