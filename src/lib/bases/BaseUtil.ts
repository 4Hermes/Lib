import { logger } from '@4lch4/logger'

/**
 * This class is the base class for all of my Utility classes. It provides a
 * common interface for all of them to extend.
 */
export class BaseUtil {
  protected logger = logger
}
